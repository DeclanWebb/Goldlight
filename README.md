
# Goldlight AI (Safe Mode)

This repository includes a legally safe, non-diagnostic AI reflection assistant.

## Setup
1. Add your OpenAI key as an environment variable:
   OPENAI_API_KEY=your_key_here

2. Install backend dependencies:
   cd backend
   npm install
   node server.js

This AI provides educational reflection only.
